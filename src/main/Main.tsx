import React from 'react';
import StartPage from './startPage/StartPage';

import './main.scss';

const Main = () => {
  return (
    <main className={'main'}>
      <StartPage />
    </main>
  );
};

export default Main;
